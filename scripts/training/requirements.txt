# Installed by SageMaker from source_dir (scripts/training).
# Pin torch to the HuggingFace estimator pytorch_version (2.2.0).
torch==2.2.0
transformers>=4.45.0
datasets>=2.12.0
accelerate>=0.22.0
peft>=0.14.0,<0.15.0
numpy>=1.24.0
tqdm>=4.65.0
pyyaml>=6.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
# Note: trl==0.15.2 is pinned in pyproject.toml [alignment] for local GKD / trajectory distillation;
# SageMaker 4.26/1.13 image remains a non-goal (ADR 0008).

