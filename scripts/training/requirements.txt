# Installed by SageMaker from source_dir (scripts/training).
# Keep peft==0.4.0: training-image compatibility pin, not a silent upgrade.
torch>=2.0.0
transformers>=4.30.0
datasets>=2.12.0
accelerate>=0.22.0
peft==0.4.0
numpy>=1.24.0
tqdm>=4.65.0
pyyaml>=6.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
