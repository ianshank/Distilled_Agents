# Deprecated nested tox.ini. Run `tox` from the repository root.
[tox]
envlist = py311
skipsdist = true

[testenv]
changedir = {toxinidir}/..
deps = .[dev]
commands = pytest -m "unit or integration" {posargs}
