# Standing facts

This workspace uses the gated product + SDLC + ML pack.

- Contract: `config/workflow-contract.json`
- Custom agents: `.agents/agents/`
- Skills: `.agents/skills/` (slash commands)
- Hooks: `.agents/hooks.json`
- Allowed writes: `artifacts/`, `openspec/changes/`, `docs/`
- Select `delivery` as the main agent for implementation. Select `product-intake` to start a change.
- Keep Artifact Review Policy on Request Review
